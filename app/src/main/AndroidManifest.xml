<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.felipe.parrot">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".activity.application.ParrotApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Parrot" >

        <activity android:name=".activity.activity.SplashScreenActivity"
            android:theme="@style/SplashScreen">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activity.activity.LoginActivity"/>

        <activity android:name=".activity.activity.RegisterActivity"/>

        <activity android:name=".activity.activity.MainActivity"/>

        <activity android:name=".activity.activity.CreateContact" />

    </application>

</manifest>